apiVersion: v1
kind: Service
metadata:
  name: recommendations
  labels:
    app: recommendations
spec:
  selector:
    app: recommendations
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
